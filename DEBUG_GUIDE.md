# 🔍 Руководство по отладке UZUM Dashboard

## Проблемы

Вы сообщили о следующих проблемах:

1. ❌ **Не отображаются остатки** (stocks) в dashboard
2. ❌ **Не показываются данные по расходам** (expenses) в dashboard  
3. ❌ **Недельный обзор не работает** (weekly overview)
4. ❌ **Данные выходят за пределы экрана** на мобильных устройствах (нет адаптивности)

## Этап 1: Проверка API через веб-интерфейс

### Самый быстрый способ - HTML Debug Tool

Я создал специальный HTML-инструмент для отладки: `/public/debug.html`

**Как использовать:**

1. Dev сервер уже запущен на http://localhost:5174
2. Откройте в браузере: **http://localhost:5174/debug.html**
3. Введите ваши учетные данные UZUM (токен и Shop ID)
4. Нажмите "Сохранить учетные данные"
5. Нажмите "Запустить все тесты"
6. **ОТКРОЙТЕ КОНСОЛЬ БРАУЗЕРА (F12)** - там будут детальные логи!

Этот инструмент покажет:
- ✅ Загружаются ли остатки (stocks)
- ✅ Загружаются ли финансовые заказы (finance orders)
- ✅ Загружаются ли расходы (expenses)
- ✅ В каком формате API возвращает данные
- ✅ Есть ли данные вообще

## Этап 2: Проверка API через Node.js скрипт

Если веб-интерфейс недоступен, можно использовать Node.js скрипт.

### Использование test_uzum_debug.mjs

```bash
# Способ 1: Через переменные окружения
UZUM_TOKEN=ваш_токен UZUM_SHOP_ID=123456 node test_uzum_debug.mjs

# Способ 2: Отредактировать скрипт напрямую
# Откройте test_uzum_debug.mjs и вставьте данные:
# const TOKEN = 'ваш_реальный_токен';
# const SHOP_ID = '12345';
```

Скрипт проверит:
- Stocks API (`/v2/fbs/sku/stocks`)
- Finance Orders API (`/v1/finance/orders`)
- Finance Expenses API (`/v1/finance/expenses`)

## Этап 3: Проверка логов в приложении

### Открыть приложение и проверить консоль

1. Откройте http://localhost:5174 в браузере
2. Откройте DevTools (F12)
3. Перейдите во вкладку Console
4. Войдите в UZUM интеграцию
5. Посмотрите логи - в коде UzumDashboard есть ОЧЕНЬ подробное логирование:

**Логи для остатков:**
```
📊 [getFbsSkuStocks] Raw API response: ...
📊 [getFbsSkuStocks] Parsed stocks: { success: ..., count: ... }
📦 ✅ Calculated warehouse stocks: { fbo: X, fbs: Y, dbs: Z }
```

**Логи для финансов:**
```
📊 Loading finance data
💰 Finance orders loaded: X orders
💰 Filtered orders for period: Y orders
💸 Expenses loaded: Z expenses
💸 Filtered expenses for period: W expenses
💸 ✅ Expenses by category: { marketing: ..., commission: ..., ... }
```

**Логи для недельного обзора:**
```
📊 Loading weekly data for range: { start: ..., end: ... }
📊 Finance orders page 0: ...
```

## Этап 4: Что искать в логах

### Возможные причины проблем:

#### 1. Остатки не показываются

**Проверьте в консоли:**
- Есть ли лог `📊 [getFbsSkuStocks] Raw API response:`?
- Если да - какая структура данных?
- Есть ли лог `📊 [getFbsSkuStocks] Parsed stocks: { success: true, count: X }`?
- Если count = 0, значит API вернул пустой массив (нет товаров)
- Есть ли лог `📦 ✅ Calculated warehouse stocks`?

**Возможные проблемы:**
- ❌ API возвращает ошибку 401/403 (неверный токен)
- ❌ API возвращает пустой массив (нет товаров в системе)
- ❌ API изменил структуру ответа (данные в другом поле)
- ❌ Прокси не работает

#### 2. Расходы не показываются

**Проверьте в консоли:**
- Есть ли лог `📊 Loading finance data`?
- Есть ли лог `💸 Expenses loaded: X`?
- Если X = 0, значит API вернул пустой массив
- Есть ли лог `💸 Filtered expenses for period: Y`?
- Если Y = 0, значит нет расходов в выбранном периоде

**Возможные проблемы:**
- ❌ Нет расходов в последние 7 дней
- ❌ API изменил структуру ответа для expenses
- ❌ Фильтрация по датам работает неправильно
- ❌ Поле `dateCreated` отсутствует в данных

#### 3. Недельный обзор не работает

**Проверьте:**
- Открывается ли компонент UzumWeeklyChart?
- Есть ли лог `📊 Loading weekly data for range:`?
- Есть ли лог `📊 Finance orders page 0:`?

**Возможные проблемы:**
- ❌ Компонент не открывается (кнопка не работает)
- ❌ Нет заказов за неделю
- ❌ Ошибка при загрузке данных

#### 4. Данные выходят за экран на мобильном

**Это проблема адаптивности CSS.**

В UzumDashboard.tsx используется:
```tsx
gridTemplateColumns: window.innerWidth > 640 ? 'repeat(3, 1fr)' : 'repeat(3, 1fr)'
```

Проблема: даже на мобильных показывается 3 колонки!

**Нужно исправить:**
- Использовать `repeat(auto-fit, minmax(200px, 1fr))`
- Добавить `overflow-x: auto` для таблиц
- Уменьшить шрифты на мобильных
- Добавить padding/margins для мобильных

## Этап 5: Сбор информации для исправления

После проверки логов, соберите следующую информацию:

### Для проблемы с остатками:
1. Возвращает ли API данные? (да/нет)
2. Сколько товаров в ответе?
3. Какая структура ответа? (Array, {items: []}, {stocks: []}, {data: []})
4. Есть ли поле `warehouseType` в товарах?
5. Есть ли поле `amount` или `stock` или `quantity`?

### Для проблемы с расходами:
1. Возвращает ли API данные? (да/нет)
2. Сколько расходов в ответе?
3. Какая структура ответа? ({payload: {payments: []}}, Array, {expenses: []})
4. Есть ли поле `dateCreated` или `createdAt`?
5. Есть ли поля `paymentPrice`, `amount`, `price`, `sum`?

### Для проблемы с недельным обзором:
1. Открывается ли компонент?
2. Есть ли заказы за неделю?
3. Какие ошибки в консоли?

### Для проблемы с мобильной адаптивностью:
1. Какие именно блоки выходят за экран?
2. На каких страницах? (dashboard, orders, products, reports?)
3. Скриншоты приветствуются

## Этап 6: Ждем вашего отчета

После выполнения проверок, пожалуйста, предоставьте:

1. **Скриншоты консоли браузера** с логами
2. **Результаты из debug.html** (скопировать вывод)
3. **Скриншоты проблемных мест** на мобильном устройстве
4. **Ответы на вопросы** из Этапа 5

С этой информацией я смогу точно определить проблему и исправить её!

## Быстрая команда для проверки

Если у вас есть доступ к терминалу и учетным данным:

```bash
# 1. Убедитесь что dev сервер запущен
npm run dev

# 2. Откройте debug tool в браузере
open http://localhost:5174/debug.html

# 3. Или запустите Node.js скрипт
UZUM_TOKEN=xxx UZUM_SHOP_ID=123 node test_uzum_debug.mjs
```

## Файлы для отладки

Я создал следующие файлы:

1. **/public/debug.html** - Веб-интерфейс для отладки API
2. **/test_uzum_debug.mjs** - Node.js скрипт для проверки API
3. **/test_uzum_dashboard.mjs** - Расширенный тест (если нужен)
4. **Этот файл (DEBUG_GUIDE.md)** - Руководство по отладке

---

**Важно:** Откройте консоль браузера (F12) - там уже есть детальное логирование!
