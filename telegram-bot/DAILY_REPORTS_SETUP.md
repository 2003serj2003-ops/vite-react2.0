# Настройка ежедневных отчетов Uzum Seller Bot

## 📋 Описание

Система автоматически отправляет ежедневные отчеты всем пользователям с активной интеграцией Uzum Market каждый день в 7:00 утра по времени Ташкента.

## 📊 Содержание отчета

Отчет включает данные за вчерашний день:

- **Принятые заказы**: количество, товары, выручка, прибыль
- **Выданные заказы**: количество, выручка, прибыль
- **Отмененные заказы**: количество и сумма
- **Выплаты**: суммы выплат за вчера и сегодня
- **Расходы**: штрафы FBS и логистика
- **Возвраты**: суммы возвратов

## 🚀 Установка

### 1. Установите зависимости

```bash
cd telegram-bot
npm install
```

Будут установлены:
- `node-telegram-bot-api` - для работы с Telegram Bot API
- `@supabase/supabase-js` - для работы с базой данных
- `node-cron` - для планирования задач

### 2. Настройте переменные окружения

Создайте файл `.env` в папке `telegram-bot`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
SUPABASE_URL=https://ykbouygdeqrohizeqlmc.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key
MINI_APP_URL=https://your-app.vercel.app
```

### 3. Запустите бота

```bash
npm start
```

Или для разработки с автоперезагрузкой:

```bash
npm run dev
```

## ⏰ Расписание

По умолчанию отчеты отправляются:
- **Время**: 7:00 утра
- **Часовой пояс**: Asia/Tashkent (UTC+5)
- **Частота**: Каждый день

Чтобы изменить время, отредактируйте cron выражение в `daily-reports.js`:

```javascript
cron.schedule('0 7 * * *', () => {
  // Минута Час День Месяц День_недели
  // 0 7 * * * = каждый день в 7:00
```

Примеры других расписаний:
- `0 8 * * *` - каждый день в 8:00
- `0 9 * * 1` - каждый понедельник в 9:00
- `0 */6 * * *` - каждые 6 часов

## 🧪 Тестирование

### Тест отправки отчета

Раскомментируйте строку в конце `daily-reports.js`:

```javascript
if (require.main === module) {
  setupDailyReports();
  
  // Тестовая отправка
  sendDailyReports(); // <-- раскомментировать
}
```

Затем запустите:

```bash
node daily-reports.js
```

### Тест генерации отчета для конкретного пользователя

```javascript
const { generateDailyReport } = require('./daily-reports');

// Замените на реальные данные
const userId = 'uuid-пользователя';
const token = 'uzum-api-token';

generateDailyReport(userId, token)
  .then(report => console.log(report))
  .catch(error => console.error(error));
```

## 📝 Логи

Система пишет подробные логи:

```
✅ Планировщик ежедневных отчетов запущен (каждый день в 7:00)
⏰ Запуск ежедневных отчетов (7:00)
📊 Начинаю отправку ежедневных отчетов...
📤 Найдено 5 активных интеграций
✅ Отчет отправлен пользователю uuid-123 (telegram-id)
✅ Отправка ежедневных отчетов завершена
```

## 🔒 Безопасность

**Важно**: В текущей версии токены хранятся в зашифрованном виде, но расшифровка не реализована.

Для продакшена нужно:

1. Реализовать расшифровку токена:

```javascript
const { decryptToken } = require('../src/lib/crypto');

// В функции sendDailyReports
const token = decryptToken(integration.token_encrypted, userPin);
```

2. Хранить PIN пользователя безопасно (можно использовать KMS)

## 🐛 Обработка ошибок

Система обрабатывает ошибки для каждого пользователя отдельно:
- Если отчет для одного пользователя не отправился, система продолжит работу для других
- Все ошибки логируются с подробностями

## 📦 Структура данных

### Таблица `integrations`

```sql
user_id UUID
integration_name TEXT (='uzum')
token_encrypted TEXT
active BOOLEAN
```

### Таблица `users`

```sql
id UUID
telegram_id BIGINT
```

## 🔄 Обновление

После изменений в коде:

1. Остановите бота (Ctrl+C)
2. Перезапустите: `npm start`

Планировщик автоматически перезапустится с новыми настройками.

## 💡 Дополнительно

### Отключение отчетов для пользователя

Обновите запись в базе данных:

```sql
UPDATE integrations 
SET active = false 
WHERE user_id = 'uuid-пользователя';
```

### Ручная отправка отчетов

В коде бота добавьте команду:

```javascript
bot.onText(/\/report/, async (msg) => {
  await sendDailyReports();
  await bot.sendMessage(msg.chat.id, '✅ Отчеты отправлены');
});
```

## 📞 Поддержка

При возникновении проблем проверьте:

1. ✅ Переменные окружения установлены правильно
2. ✅ База данных доступна
3. ✅ Бот имеет доступ к отправке сообщений пользователям
4. ✅ У пользователей есть активная интеграция с `active = true`
5. ✅ У пользователей указан `telegram_id`

## 🎯 Следующие шаги

- [ ] Добавить возможность настройки времени отчета для каждого пользователя
- [ ] Добавить выбор языка отчета (RU/UZ)
- [ ] Добавить графики и визуализацию данных
- [ ] Добавить уведомления о проблемах (низкие продажи, много отмен)
- [ ] Добавить сравнение с предыдущим периодом
