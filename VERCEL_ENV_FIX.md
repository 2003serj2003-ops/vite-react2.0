# 🔧 РЕШЕНИЕ: Environment Variable Error на Vercel

## 🔴 ПРОБЛЕМА
```
Environment Variable "VITE_SUPABASE_URL" references Secret "vite_supabase_url", which does not exist.
```

Это значит, что переменная настроена неправильно - она ссылается на Secret, который не существует.

---

## ✅ РЕШЕНИЕ

### Шаг 1: Откройте Vercel Settings

1. Откройте https://vercel.com
2. Выберите проект **vite-react2.0**
3. **Settings** (в верхнем меню)
4. Левое меню → **Environment Variables**

### Шаг 2: Удалите неправильные переменные

1. Найдите переменную с ошибкой:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
   - `VITE_TELEGRAM_BOT_TOKEN`
   - `VITE_TELEGRAM_CHAT_ID`

2. Нажмите на неё (или на **⋮ три точки** справа)
3. Выберите **Delete** (Удалить)

### Шаг 3: Добавьте переменные заново ПРАВИЛЬНО

Важно: Добавляйте как **обычные переменные**, НЕ как Secrets!

**Переменная 1:**
- Name: `VITE_SUPABASE_URL`
- Value: `https://ykbouygdeqrohizeqlmc.supabase.co`
- Нажмите **Add** (не Save Secret!)

**Переменная 2:**
- Name: `VITE_SUPABASE_ANON_KEY`
- Value: `sb_publishable_6sJ_KOewkD5zRln2HVDWXw_vjILs-kD`
- Нажмите **Add**

**Переменная 3:**
- Name: `VITE_TELEGRAM_BOT_TOKEN`
- Value: `8274387256:AAENRm4uxyQ91s4g8RfmEhq3SxnKzH8Kzvs`
- Нажмите **Add**

**Переменная 4:**
- Name: `VITE_TELEGRAM_CHAT_ID`
- Value: `-1001234567890` (замените на ваш правильный Chat ID)
- Нажмите **Add**

### Шаг 4: Redeploy

1. Перейдите на вкладку **Deployments**
2. Нажмите на последний деплой
3. Внизу нажмите **Redeploy**
4. Подтвердите

Дождитесь статуса **Ready** ✓

---

## ⚠️ ВАЖНО

- **НЕ используйте "Save Secret"** - это создаёт Secret, на который потом нужно ссылаться
- **Используйте простое "Add"** - это добавит переменную напрямую
- Все VITE_ переменные должны быть обычные переменные, не Secrets

---

## ✅ ПРОВЕРКА

После редеплоя откройте https://vite-react20.vercel.app и проверьте:
- Работает ли приложение?
- Нет ли ошибок?
- Видите ли данные (новости, разделы)?

