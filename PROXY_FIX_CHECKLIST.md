# ✅ Чеклист проверки исправления прокси Uzum API

## Что было исправлено

### 🔧 Изменения в архитектуре
1. **src/lib/uzum-api.ts**: Все запросы теперь передают путь через заголовок `X-Uzum-Path`
2. **vite.config.ts**: Прокси читает путь из заголовка и корректно проксирует на Uzum API
3. **functions/api/uzum-proxy.js**: Cloudflare функция обрабатывает все HTTP методы (GET/POST/PUT/DELETE)

### 🎯 Ключевые моменты
- ✅ Авторизация передается БЕЗ префикса "Bearer" (только токен)
- ✅ Параметр `shopIds` (не `shopId`) для FBS endpoints
- ✅ Единый механизм в DEV и PROD

---

## 📋 Проверка DEV (localhost)

### 1. Запуск dev-сервера
```bash
npm run dev
```

### 2. Тест через скрипт
```bash
node test_proxy_fix.mjs <ваш_токен> <shop_id>
```

**Пример:**
```bash
node test_proxy_fix.mjs "eyJhbGc..." 96273
```

**Ожидаемый результат:**
```
1️⃣ Прямой запрос к Uzum API (без прокси):
   ✅ getShops(): OK, магазинов: 1
   ✅ getFbsOrdersCount(COMPLETED): OK, заказов: 42

2️⃣ Запрос через локальный прокси:
   ✅ getShops(): OK, магазинов: 1
   ✅ getFbsOrdersCount(COMPLETED): OK, заказов: 42
```

### 3. Тест в приложении
1. Откройте http://localhost:5173
2. Перейдите в раздел **Uzum**
3. Вставьте токен в поле
4. Нажмите **"Test token"** или **"Connect"**
5. Откройте **DevTools (F12) → Console**

**Ожидаемые логи:**
```
🏪 Raw shops API response: [{ id: 96273, ... }]
🏪 Parsed shops: [...]
📋 Raw orders count API response: 42
📋 Parsed count: 42
```

**НЕ должно быть:**
- ❌ CORS errors
- ❌ 404 Not Found
- ❌ Network errors

---

## 🚀 Проверка PROD (Cloudflare Pages)

### 1. Деплой
```bash
npm run build
npx wrangler pages deploy dist
```

Или через GitHub (автоматический деплой).

### 2. Тест в production
1. Откройте production URL (например, `https://your-app.pages.dev`)
2. Перейдите в раздел **Uzum**
3. Вставьте токен
4. Нажмите **"Test token"**
5. Проверьте **DevTools → Network**

**Проверьте запросы:**
- URL: `/api/uzum-proxy`
- Method: `GET`
- Headers:
  - `X-Uzum-Path: /v1/shops`
  - `Authorization: <ваш_токен>`
- Status: `200 OK`

### 3. Проверка Cloudflare Functions
В Cloudflare Dashboard → Pages → Функции → Логи:
```
✅ Function invoked: /api/uzum-proxy
✅ Status: 200
```

---

## 🧪 Основные тест-кейсы

### Кейс 1: Получение магазинов
**Endpoint:** `GET /v1/shops`
**Ожидание:** Массив магазинов

### Кейс 2: Количество заказов COMPLETED
**Endpoint:** `GET /v2/fbs/orders/count?shopIds=96273&status=COMPLETED`
**Ожидание:** Число (например, 42)

### Кейс 3: Список заказов
**Endpoint:** `GET /v2/fbs/orders?shopIds=96273&size=20&page=0`
**Ожидание:** Массив заказов

### Кейс 4: Обновление цен
**Endpoint:** `POST /v1/product/{shopId}/sendPriceData`
**Body:** `{ prices: [{ sku: "123", price: 100000 }] }`
**Ожидание:** `200 OK`

---

## 🐛 Если что-то не работает

### DEV (localhost)
```bash
# 1. Проверьте, что dev-сервер запущен
npm run dev

# 2. Проверьте консоль браузера (F12)
# Должны быть логи вида: 🏪 Raw shops API response: ...

# 3. Проверьте Network (F12)
# Запрос должен идти на /api/uzum-proxy, а не напрямую на api-seller.uzum.uz

# 4. Очистите кеш браузера
Ctrl+Shift+R (Windows/Linux) или Cmd+Shift+R (Mac)
```

### PROD (Cloudflare)
```bash
# 1. Проверьте, что функция задеплоена
ls functions/api/uzum-proxy.js

# 2. Проверьте логи в Cloudflare Dashboard
Pages → <ваш_проект> → Functions → Real-time Logs

# 3. Пересоберите и передеплойте
npm run build
npx wrangler pages deploy dist
```

---

## 📊 Diff файлов

### src/lib/uzum-api.ts
- Убран POST с JSON body для прокси
- Добавлен заголовок `X-Uzum-Path` с целевым endpoint
- Используется метод из `options.method`

### vite.config.ts
- Убран простой `rewrite`
- Добавлен `configure` с `proxy.on('proxyReq')`
- Чтение пути из заголовка `X-Uzum-Path`

### functions/api/uzum-proxy.js
- Изменен с `onRequestPost` на `onRequest` (все методы)
- Чтение пути из заголовка `X-Uzum-Path`
- Проброс всех HTTP методов (GET/POST/PUT/DELETE)

---

## ✨ Итог

После исправлений:
- ✅ DEV и PROD используют одинаковый механизм
- ✅ Нет дублирования логики
- ✅ Правильная передача авторизации (без "Bearer")
- ✅ Корректная работа с параметром `shopIds`
- ✅ Поддержка всех HTTP методов

Все вызовы Uzum API теперь проходят через единый прокси `/api/uzum-proxy`! 🎉
