# Uzum API - Инструкция по тестированию

## Подтвержденная информация из Swagger

✅ **Базовый URL**: `https://api-seller.uzum.uz/api/seller-openapi`  
✅ **Авторизация**: RAW токен без префикса Bearer  
✅ **Заголовок**: `Authorization: <your-token>`  

## Официальные эндпоинты

### Shop
- `GET /v1/shops` - Получение списка собственных магазинов

### Product
- `GET /v1/product/shop/{shopId}` - Получение SKU по ID магазина
- `POST /v1/product/{shopId}/sendPriceData` - Изменение цен SKU

### FBS Orders
- `GET /v2/fbs/orders` - Получение заказов продавца
- `GET /v2/fbs/orders/count` - Получить количество заказов
- `GET /v1/fbs/order/{orderId}` - Получение информации о заказе
- `POST /v1/fbs/order/{orderId}/confirm` - Подтверждение заказа
- `POST /v1/fbs/order/{orderId}/cancel` - Отмена заказа
- `GET /v1/fbs/order/{orderId}/labels/print` - Получить этикетку
- `GET /v1/fbs/order/return-reasons` - Получение причин возврата

### FBS Stocks
- `GET /v2/fbs/sku/stocks` - Получение остатков по SKU
- `POST /v2/fbs/sku/stocks` - Обновление остатков по SKU

### Finance
- `GET /v1/finance/orders` - Получение списка заказов
- `GET /v1/finance/expenses` - Получение списка расходов продавца

### Invoice & Returns
- `GET /v1/invoice` - Получение списка накладных
- `GET /v1/return` - Получение возвратов продавца
- `GET /v1/shop/{shopId}/invoice` - Получение накладных поставки по ID магазина
- `GET /v1/shop/{shopId}/invoice/products` - Получение состава накладной
- `GET /v1/shop/{shopId}/return` - Получение накладных возврата
- `GET /v1/shop/{shopId}/return/{returnId}` - Получение состава накладной возврата

## Как тестировать

### Автоматическое тестирование в приложении

1. Откройте http://localhost:5174/
2. Откройте **DevTools Console** (F12)
3. Перейдите на страницу **Uzum** из бокового меню
4. В консоли появятся детальные логи:

```
🔍 Testing token with official endpoint: /v1/shops
📝 Token length: ...
📝 Token preview: ...
📝 Full URL will be: https://api-seller.uzum.uz/api/seller-openapi/v1/shops
🔵 API Request: { endpoint, fullUrl, useProxy, method, authScheme, tokenPrefix }
```

### Если основной эндпоинт вернет 404

Автоматически запустится **полная диагностика**:

```
💡 Running full API diagnosis...
🔬 Starting comprehensive API diagnosis...
📋 Token info: { length, preview, authScheme }

🔍 Testing: https://api-seller.uzum.uz/api/seller-openapi/v1/shops
📤 Request headers: { Authorization, Accept, Content-Type }
📥 Response: { status, statusText, contentType }
```

Диагностика протестирует **12 комбинаций**:
- 4 базовых URL
- 3 варианта эндпоинтов
- С подробными заголовками и ответами

### Ручное тестирование через Console

В консоли браузера можно вызвать:

```javascript
// Импортируйте функции
import { quickTest, testEndpoint, diagnoseApi } from './lib/uzum-api';

// Быстрый тест всех функций
quickTest('your-uzum-token-here');

// Тест конкретного эндпоинта
testEndpoint('your-token', '/v1/shops');

// Полная диагностика всех URL
diagnoseApi('your-token');
```

## Что означают статусы

- **200 OK** ✅ - Токен работает, данные получены
- **401 Unauthorized** 🔒 - Неверный токен
- **403 Forbidden** 🚫 - Токен не имеет прав доступа
- **404 Not Found** ❌ - Эндпоинт не существует (возможно, токен не активирован)
- **500 Server Error** 💥 - Ошибка на стороне Uzum

## Возможные причины 404 для всех эндпоинтов

1. **Токен не активирован** в личном кабинете Uzum Seller
   - Зайдите в настройки магазина на uzum.uz
   - Найдите раздел "API интеграция" или "Разработчикам"
   - Активируйте API доступ

2. **Токен истек**
   - Сгенерируйте новый токен в личном кабинете

3. **API требует регистрации приложения**
   - Возможно нужно зарегистрировать приложение через техподдержку Uzum

4. **Неправильный токен**
   - Убедитесь, что скопировали токен полностью
   - Проверьте, нет ли лишних пробелов

## Проверка токена вручную через curl

```bash
curl -X GET "https://api-seller.uzum.uz/api/seller-openapi/v1/shops" \
  -H "Authorization: YOUR_TOKEN_HERE" \
  -H "Accept: application/json" \
  -H "Content-Type: application/json"
```

Если curl возвращает 404 - значит проблема не в нашем коде, а в токене или его активации.

## Поддержка

Если все тесты показывают 404:
1. Проверьте активацию API в личном кабинете Uzum
2. Обратитесь в техподдержку Uzum Seller
3. Уточните процесс получения доступа к API
